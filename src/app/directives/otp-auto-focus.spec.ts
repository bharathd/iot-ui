import { OtpAutoFocus } from './otp-auto-focus';

describe('OtpAutoFocus', () => {
  it('should create an instance', () => {
    const directive = new OtpAutoFocus();
    expect(directive).toBeTruthy();
  });
});
